//pseudocodigo
graph TD;
    A[Inicio] --> B[Para i = 1 hasta 12]
    B --> C[Para j = 1 hasta 12]
    C --> D[Imprimir i + j]
    D --> C
    C --> E[Fin Para]
    E --> B
    B --> F[Fin Para]
    F --> G[Fin]
// Programa en ensamblador ARM64 para encontrar la suma de 35 enteros.

.section .data
input_msg:  .asciz "Introduce el entero: "
fmt_input:  .asciz "%d"
fmt_output: .asciz "Suma = %d\n"

.section .text
.global _start
_start:
    mov x0, #0            // Inicializa suma = 0
    mov x1, #1            // Inicializa contador = 1

loop:
    cmp x1, #36           // Compara el contador con 36
    bge end               // Si contador >= 36, termina el bucle

    // Imprime mensaje para solicitar entrada
    mov x0, input_msg
    bl printf

    // Lee entero ingresado por el usuario
    mov x0, fmt_input
    bl scanf
    mov x2, x0            // Almacena el valor ingresado

   
